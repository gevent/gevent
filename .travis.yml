language: python
sudo: false
env:
    - TOXENV=travis-lint
    - TOXENV=py26
    - TOXENV=py27
    - TOXENV=pypy
    - TOXENV=py33
    - TOXENV=py34
install:
    - travis_retry pip install -U pip
    - travis_retry pip install -U tox cython
script:
    - tox
notifications:
  email: false
